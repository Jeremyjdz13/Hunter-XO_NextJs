import { useAuth } from '../api/AuthContext'


export default function Characters() {
    // const { currentUser } = useAuth();
    // if (!currentUser) {
    //   return null;
    // }

    return (
      <div>
       Characters
      </div>
    );
  }