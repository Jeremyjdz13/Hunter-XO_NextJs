import { useAuth } from '../api/AuthContext'


export default function Journal() {
    // const { currentUser } = useAuth();
    // if (!currentUser) {
    //   return null;
    // }

    return (
      <div>
       Journal
      </div>
    );
  }